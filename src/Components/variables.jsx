const BaseURL='https://api.tvmaze.com/'
const show_search=`${BaseURL}search/shows?q=`;
const actor_search=`${BaseURL}search/people?q=`
const shows_id=`${BaseURL}shows/`
const actor_id=`${BaseURL}people/`


export {show_search,actor_id,BaseURL,shows_id,actor_search}

